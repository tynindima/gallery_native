const initialState = {
  photos: [],
  isModal: false,
  fullPhoto: '',
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case '':
      return {};
    default:
      return state;
  }
};
